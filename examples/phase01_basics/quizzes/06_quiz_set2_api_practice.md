# 第一阶段：API 实战应用测验（第2套）

> 测试时间：2025-02-08
> 重点：API 使用和实际应用

---

## 📝 使用说明

1. **先做题**：在每题的"你的答案"处填写选项（A/B/C/D）
2. **做完再查看答案**：滚动到文档最后，查看正确答案和解析
3. **统计得分**：看看自己得了多少分

---

## 第一部分：图的基础操作（5题）

### 题目1：创建一个图，正确的代码是？

A. `graph = new Graph(MyState)`
B. `graph = StateGraph()`
C. `graph = StateGraph(MyState)`
D. `graph = Graph(MyState)`

**你的答案**：___C

---

### 题目2：添加节点时，参数的正确顺序是？

A. `graph.add_node(函数, 节点名)`
B. `graph.add_node(节点名, 函数)`
C. `graph.add(节点名, 函数)`
D. `graph.node(节点名, 函数)`

**你的答案**：___B

---

### 题目3：编译图的正确方法是？

A. `app = graph.build()`
B. `app = graph.compile()`
C. `app = graph.create()`
D. `app = graph.make()`

**你的答案**：___B

---

### 题目4：运行图的正确方法是？

A. `app.run(state)`
B. `app.execute(state)`
C. `app.invoke(state)`
D. `app.start(state)`

**你的答案**：___C

---

### 题目5：`invoke()` 的第一个参数应该是什么？

A. 用户输入的字符串
B. 状态字典
C. 节点名称
D. 配置对象

**你的答案**：___B

---

## 第二部分：边的连接（5题）

### 题目6：连接节点 A 到节点 B，使用哪个方法？

A. `graph.connect(A, B)`
B. `graph.link(A, B)`
C. `graph.add_edge(A, B)`
D. `graph.path(A, B)`

**你的答案**：___C

---

### 题目7：从起点连接到 "my_node"，正确的代码是？

A. `graph.add_edge(my_node, START)`
B. `graph.add_edge(START, "my_node")`
C. `graph.connect(START, "my_node")`
D. `graph.link("my_node", START)`

**你的答案**：___B

---

### 题目8：条件边使用的函数返回值应该是什么？

A. 节点函数
B. 节点名称（字符串）
C. 布尔值（True/False）
D. END 常量

**你的答案**：___B

---

### 题目9：`add_conditional_edges()` 需要几个必须参数？

A. 1个（源节点）
B. 2个（源节点、目标节点）
C. 2个（源节点、条件函数）
D. 3个（源节点、条件函数、路径映射）

**你的答案**：___C

---

### 题目10：条件函数返回 END 时表示什么？

A. 删除图
B. 结束执行
C. 跳过当前节点
D. 重新开始

**你的答案**：___B

---

## 第三部分：节点函数实现（5题）

### 题目11：节点函数接收的参数类型是？

A. 字典
B. 列表
C. 字符串
D. 整数

**你的答案**：___A

---

### 题目12：节点函数返回值的格式应该是？

A. `return state`
B. `return {"key": value}`
C. `return "complete"`
D. 不需要返回

**你的答案**：___B

---

### 题目13：从 state 中获取 messages，正确的代码是？

A. `state.get("messages")`
B. `state["messages"]`
C. `state.messages`
D. `state.get_messages()`

**你的答案**：___B

---

### 题目14：如何获取最后一条消息？

A. `state["messages"].last()`
B. `state["messages"][-1]`
C. `state["messages"].last`
D. `state["messages"].end()`

**你的答案**：___B

---

### 题目15：检查最后一条消息是否有 `tool_calls`，正确的代码是？

A. `state["messages"][-1].tool_calls`
B. `hasattr(state["messages"][-1], "tool_calls")`
C. `hasattr(last_msg, "tool_calls") and last_msg.tool_calls`
D. 以上都不对

**你的答案**：___C

---

## 第四部分：LLM 配置和工具（5题）

### 题目16：配置智谱 GLM 时，应该使用哪个类？

A. `ChatZhipu`
B. `ChatOpenAI`
C. `ChatAnthropic`
D. `ChatGoogle`

**你的答案**：___B

---

### 题目17：智谱 API 的 `base_url` 是什么？

A. `https://api.zhipu.cn`
B. `https://open.bigmodel.cn/api/paas/v4/`
C. `https://zhipu.com/api`
D. `https://glm.zhipu.ai`

**你的答案**：___B

---

### 题目18：使用 YAML + Pydantic 管理配置，Pydantic 的 BaseModel 是什么？

A. AI 模型基类
B. 数据验证模型
C. 图模型基类
D. 节点模型基类

**你的答案**：___B

---

### 题目19：`**data` 的作用是什么？

A. 解包字典为参数
B. 打包参数为字典
C. 创建新字典
D. 删除字典

**你的答案**：___A

---

### 题目20：`Optional[str]` 在 YAML 配置中表示什么？

A. 必须填写字符串
B. 可以填字符串或不填
C. 只能填数字
D. 必须填数字

**你的答案**：___B

---

## 📊 正确答案与解析

### 第一部分：图的基础操作

**题目1：C**
- **解析**：`StateGraph(MyState)` 正确，传入状态类作为参数

**题目2：B**
- **解析**：`graph.add_node("节点名", 函数)` - 节点名在前，函数在后

**题目3：B**
- **解析**：`graph.compile()` 编译图

**题目4：C**
- **解析**：`app.invoke(state)` 运行图

**题目5：B**
- **解析**：`invoke()` 的第一个参数是状态字典

### 第二部分：边的连接

**题目6：C**
- **解析**：`graph.add_edge("A", "B")` 连接节点

**题目7：B**
- **解析**：`graph.add_edge(START, "my_node")` 从起点开始

**题目8：B**
- **解析**：条件函数返回节点名称（字符串）或 END

**题目9：C**
- **解析**：必须参数是源节点和条件函数，路径映射可选

**题目10：B**
- **解析**：返回 END 表示结束执行

### 第三部分：节点函数实现

**题目11：A**
- **解析**：节点函数接收状态字典

**题目12：B**
- **解析**：返回字典，包含需要更新的字段

**题目13：B**
- **解析**：`state["messages"]` 访问字典字段

**题目14：B**
- **解析**：`[-1]` 获取列表最后一个元素

**题目15：D**
- **解析**：需要先获取 `last_msg = state["messages"][-1]`，然后检查

### 第四部分：LLM 配置和工具

**题目16：B**
- **解析**：使用 `ChatOpenAI` 配合 `base_url` 可以调用兼容 OpenAI API 的服务

**题目17：B**
- **解析**：智谱 API 的 `base_url` 是 `https://open.bigmodel.cn/api/paas/v4/`

**题目18：B**
- **解析**：Pydantic 的 BaseModel 是数据验证模型，不是 AI 模型

**题目19：A**
- **解析**：`**data` 解包字典为关键字参数

**题目20：B**
- **解析**：`Optional[str]` 表示可以填字符串或不填

---

## 📈 成绩统计

### 答案卡（快速对答案）

1. C | 2. B | 3. B | 4. C | 5. B
6. C | 7. B | 8. B | 9. C | 10. B
11. A | 12. B | 13. B | 14. B | 15. D
16. B | 17. B | 18. B | 19. A | 20. B

---

### 评分标准

**得分**：___ /20

**正确率**：___%

**评价**：
- ✅ **18-20分**：优秀！API 使用非常熟练
- ⚠️ **15-17分**：良好！大部分 API 已掌握
- 📝 **12-14分**：及格，建议多动手练习
- 📚 **0-11分**：需要重新学习 API 使用
