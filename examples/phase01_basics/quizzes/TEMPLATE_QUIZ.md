<!--
📘 Quiz Markdown 格式规范

本模板遵循 Quiz Markdown 格式规范。完整规范请查看：docs/QUIZ_MARKDOWN_SPEC.md

核心规则：
1. 题目标题：### [类型:choice] 题目N：问题  或  ### [类型:open] 题目N：问题
2. 选择题选项：A. xxx  B. xxx  C. xxx  D. xxx （大写字母+点号）
3. 正确答案：**正确答案**：A
4. 解析：**解析**：详细说明

题型：choice=选择题, open=开放题
分隔：题目之间用 --- 分隔

AI 生成提示：请严格遵循上述格式，确保能被 web/app.py 的 parse_quiz() 正确解析
-->

# [测试题名称]

> 测试时间：YYYY-MM-DD
> 目的：[说明测试目的]
> 题型：选择题 / 开放性问题 / 混合
> 题量：共 X 题
> 建议用时：X 分钟

---

## 📋 答题说明

1. **选择题**：从 A/B/C/D 中选择一个最佳答案
2. **开放性问题**：在"你的答案"处写下你的理解
3. **查看答案**：每个问题后面紧跟正确答案和解析
4. **自我评估**：标记掌握情况（✅ 完全掌握 / ⚠️ 有点模糊 / ❌ 需要复习）

---

## 第一部分：[部分名称]

### [类型:choice] 题目1：MessagesState 预定义了哪个字段？

A. `input`
B. `messages`
C. `output`
D. `state`

**正确答案**：B
**解析**：MessagesState 是 LangGraph 提供的预定义状态类，它预定义了 `messages` 字段，用于存储对话历史（用户消息、AI消息、工具消息等）。

---

### [类型:choice] 题目2：哪个消息类型可以包含 `tool_calls` 字段？

A. HumanMessage
B. AIMessage
C. ToolMessage
D. SystemMessage

**正确答案**：B
**解析**：`tool_calls` 字段只在 AIMessage 中存在，用于表示 LLM 想要调用工具及参数。ToolMessage 中有对应的 `tool_call_id` 来匹配。

---

### [类型:open] 题目3：State 在 LangGraph 中的作用是什么？

**你的答案**：
_（写下你的理解）_


**正确答案**：

State 就像传送带上的包裹 📦

- **作用**：存储对话历史和应用的当前状态
- **传递**：在各个节点之间传递数据
- **更新**：每个节点可以修改它并传递给下一个节点

**代码示例**：
```python
from langgraph.graph import MessagesState

class CalculatorState(MessagesState):
    pass  # 继承 MessagesState，自动有 messages 字段

# 或者自定义状态
class MyState(TypedDict):
    input: str
    output: str
    steps: Annotated[list[str], operator.add]
```

**你的掌握情况**：
- [ ] ✅ 完全掌握
- [ ] ⚠️ 有点模糊
- [ ] ❌ 需要复习

---

## 第二部分：[部分名称]

### [类型:choice] 题目4：节点函数应该返回什么？

A. 完整的 State 字典
B. 只返回需要更新的字段
C. 返回字符串
D. 不需要返回值

**正确答案**：B
**解析**：节点函数应该只返回需要更新的字段，LangGraph 会自动将这些更新合并到完整的 State 中。这遵循了"immutable update"模式。

**示例对比**：
```python
# ❌ 错误做法
def my_node(state):
    return {"messages": [...], "count": 0, "other": ...}  # 返回整个 State

# ✅ 正确做法
def my_node(state):
    return {"messages": [...]}  # 只返回需要更新的字段
```

---

### [类型:choice] 题目5：如何定义一个条件边？

A. `graph.add_edge("a", "b")`
B. `graph.add_conditional_edges("a", route_function)`
C. `graph.add_node("a")`
D. `graph.compile()`

**正确答案**：B
**解析**：使用 `add_conditional_edges()` 方法定义条件边，传入源节点和路由函数。路由函数根据状态返回下一个节点的名称。

**代码示例**：
```python
def should_continue(state: MessagesState):
    last_message = state["messages"][-1]
    if last_message.tool_calls:
        return "tools"
    return END

graph.add_conditional_edges("llm", should_continue)
```

---

### [类型:open] 题目6：描述一个完整的工具调用流程

**你的答案**：
_（画出流程图或描述步骤）_


**正确答案**：

完整的工具调用流程：

```
用户输入 "3乘以5"
    ↓
HumanMessage(content="3乘以5")
    ↓
LLM 节点
    ↓
AIMessage(
    content="",
    tool_calls=[{
        "name": "multiply",
        "args": {"a": 3, "b": 5},
        "id": "call_001"
    }]
)
    ↓
should_continue 检测到 tool_calls
    ↓
去 ToolNode
    ↓
执行 multiply(3, 5) → 15
    ↓
ToolMessage(content=15, tool_call_id="call_001")
    ↓
回到 LLM 节点
    ↓
AIMessage(content="3乘以5等于15")
    ↓
should_continue 检测不到 tool_calls
    ↓
END
```

**关键步骤**：
1. LLM 决定调用工具 → AIMessage 包含 `tool_calls`
2. 路由到 ToolNode → 条件边检测 `tool_calls`
3. 执行工具 → ToolNode 自动调用
4. 返回结果 → ToolMessage 包含 `tool_call_id`
5. 回到 LLM → 继续处理
6. 生成最终回复 → 结束

---

## 📊 正确答案汇总

| 题号 | 答案 | 掌握情况 |
|------|------|----------|
| 1 | B | [ ] |
| 2 | B | [ ] |
| 3 | （见解析） | [ ] |
| 4 | B | [ ] |
| 5 | B | [ ] |
| 6 | （见解析） | [ ] |

---

## 📈 自我评估

### 完成情况

- **完成时间**：____ 分钟
- **正确率**：____% （选择题共 4 题）
- **开放题完成情况**：____ / 2

### 知识点掌握情况

- [ ] State 和 MessagesState
- [ ] 消息类型（HumanMessage、AIMessage、ToolMessage）
- [ ] Nodes 和节点函数
- [ ] Edges 和条件边
- [ ] tool_calls 和 tool_call_id
- [ ] ToolNode 的使用
- [ ] 完整的工具调用流程

### 需要重点复习的内容

1. [ ] ________
2. [ ] ________
3. [ ] ________

### 下一步学习

- [ ] 重新阅读相关讲义
- [ ] 完成更多练习题
- [ ] 实践编码练习
- [ ] 进入下一阶段学习

---

**注释**：本模板符合 `QUIZ_MARKDOWN_SPEC.md` 规范，可复制使用。
